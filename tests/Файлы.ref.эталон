./tests/Файлы.ref:7:33: замечание: создаётся копия переменной:
    7 |      <Putout 0 'Копируем "'e.имя'"'>
      |                                ^
./tests/Файлы.ref:8:31: замечание: создаётся копия переменной:
    8 |      <Open 'r' 1 'tests/'e.имя>
      |                              ^
./tests/Файлы.ref:9:31: замечание: создаётся копия переменной:
    9 |      <Open 'w' 2 '/tmp/' e.имя>
      |                              ^

Копируем "Файлы.ref"

go = <тест 'Файлы.ref'>;

тест {
   e.имя =
      <Putout 0>
      <Putout 0 'Копируем "'e.имя'"'>
      <Open 'r' 1 'tests/'e.имя>
      <Open 'w' 2 '/tmp/' e.имя>
      <Open 'r' 3 '/tmp/' e.имя>
      <копируй 1 в 2>
      <копируй 3 в 0>
      <Close 1>
      <Close 2>
      <Close 3>
}

в;

копируй {
* при входе нет дополнительных данных — открываем файл
   s.источник в s.приёмник = 
      <копируй (<Get s.источник>) s.источник в s.приёмник>;
* рекурсивно копируем
   (0)  s.источник в s.приёмник = ;
   (e.) s.источник в s.приёмник =
      <Putout s.приёмник e.>
      <копируй (<Get s.источник>) s.источник в s.приёмник>;
}
